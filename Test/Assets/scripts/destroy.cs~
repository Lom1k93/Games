using UnityEngine;
using System.Collections;

public class destroy : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
//destroys gameobj when space is pressed
	void Update () {
		if(Input.GetKeyDown("space")){
			Destroy (gameObject );
		}
	}
}
